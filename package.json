{
  "name": "auto-web-demowebshop",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "register": "npx cypress run --browser chrome --spec 'cypress/e2e/page-register.cy.js'",
    "regression": "npx cypress run --browser chrome --spec 'cypress/e2e/*'"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "cypress": "^13.2.0"
  }
}
